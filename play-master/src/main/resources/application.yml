server:
  port: 8082

security:
  jwt:
    token:
      secret-key: secret-key

secets:
  ui-token: token_ui
  gm-token: token_gm
  pm-token: token_pm

spring:
  application.name: PlayMasterApplication
  devtools:
    livereload:
      port: 12345