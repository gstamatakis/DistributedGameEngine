spring:
  application.name: UserInterfaceApplication
  devtools:
    livereload:
      port: 12345
  datasource:
    url: jdbc:mysql://192.168.1.100:3306/uidb
    username: user1
    password: password1
  tomcat:
    max-wait: 20000
    max-active: 50
    max-idle: 20
    min-idle: 15
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
        id:
          new_generator_mappings: false
  cloud.stream:
    kafka:
      binder:
        brokers: 127.0.0.1:9094,127.0.0.1:9095,127.0.0.1:9096
        replication-factor: 3
      streams.binder.configuration.application.server: localhost:18080

redis:
  host: 192.168.1.100
  port: 6379
  pass:
  enc_key: dkdi38393kd

playmaster.store.url: "http://localhost:8082"

server:
  port: 8080

security:
  jwt:
    token:
      secret-key: secret-key
      expire-length: 300000 # 5 minutes duration by default: 5 minutes * 60 seconds * 1000 milliseconds

UserController:
  signin: Authenticates user and returns its JWT token.
  signup: Creates user and returns its JWT token
  delete: Deletes specific user by username
  search: Returns specific user by username
  me: Returns current user's data

QueueController:
  practice: Queue up for a practice play.
  tournament: Queue up for a tournament.
  tournament_create: Create a tournament and get its ID
